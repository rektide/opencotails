{"_type":"issue","id":"cotail-directory","title":"Show session directory","description":"Add `session.directory` (fall back to `session.path`) to results.\n\n- Human view: show the directory line (e.g. alongside slug).\n- `--json`: expose a `directory` field.\n- Implement now against the existing TS prototype.\n\nNote: filtering/scoping by directory belongs to the search \u0026 scoping epic; this ticket is display-only.\n\nAcceptance: directory shown in human + json for `cotails \u003cterm\u003e` and `--title-only`.","notes":"Implemented in opencotails.ts:\n- Added `directory` to both SELECTs (buildTitleQuery: `directory`; buildPartQuery: `s.directory AS directory`).\n- renderHuman now shows `slug · directory` on the meta line (grey).\n- `--json` exposes `directory` automatically (whole row is serialized).\n\nVerified: human + --title-only + --json all surface the session directory (e.g. `/home/rektide/src/opencotails`).","status":"closed","priority":1,"issue_type":"feature","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-24T10:17:36Z","created_by":"rektide de la faye","updated_at":"2026-07-24T10:18:41Z","closed_at":"2026-07-24T10:18:41Z","close_reason":"Closed","dependencies":[{"issue_id":"cotail-directory","depends_on_id":"cotail-display","type":"parent-child","created_at":"2026-07-24T06:17:41Z","created_by":"rektide de la faye","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"cotail-display","title":"Information gathering \u0026 display","description":"Surface the metadata, timeline, and lineage around opencode sessions so a user can orient on *what a session is and how it came to be* — not just whether it matches a search term.\n\nToday `cotails` reports only `id`, `slug`, `title`, `updated`, and a text `snippet`. The opencode schema carries far richer signal that is currently invisible (all on the `session` table unless noted):\n\n- **Provenance:** `time_created` (when it began), `directory` / `path` (where), `project_id` -\u003e `project.name`.\n- **Fork lineage:** `fork_session_id` + `fork_message_id` — the parent session and the exact message at which this session diverged.\n- **Lifecycle:** `time_compacting`, `time_suspended`, `time_archived`.\n- **Cost/usage:** `cost`, `tokens_input`, `tokens_output`, `tokens_reasoning`, `tokens_cache_read`/`write`.\n\nThis epic owns **reporting** — reading these fields and rendering them in both human and `--json` output. It does NOT own the matching/query side (search \u0026 scoping epic) or the FTS index (index epic).\n\n## Scope (children)\n- `cotail-created-at` — first `time_created`.\n- `cotail-fork-time` — last fork time.\n- `cotail-fork-point` — the previous prompt+reply and fork prompt+reply around the fork boundary, with timestamps.\n- `cotail-directory` — the session directory (implement first).\n\n## Out of scope\n- Search semantics, scoping filters, FTS index, Rust port (separate epics).\n","status":"open","priority":1,"issue_type":"epic","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-24T10:17:12Z","created_by":"rektide de la faye","updated_at":"2026-07-24T10:17:12Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"cotail-history-cmd","title":"Implement cotails history command","description":"Implement `cotails history` per `.design/history-viewer/design.md`.\n\n## Behavior\n- Filter: `session.time_updated \u003e= now - since` (default `--since 24h`).\n- Optional `--directory \u003cpath\u003e` substring filter; optional `--limit \u003cn\u003e` (0 = unlimited).\n- Per session emit: id, title, directory, messages_recent, messages_total, time_updated (plus slug/time_created in machine formats).\n- Counts: sum `message` (v1) + `session_message` (v2) rows — they are mutually exclusive per session, so no mode detection is needed. In-window count uses `time_created \u003e= cutoff`. Both hit the existing `(session_id, time_created, id)` indexes.\n- Sort by `time_updated DESC`.\n\n## Output\n- default: human `table` (aligned, ANSI if TTY, truncate with ellipsis, footer count line).\n- `--json`: JSONL, one object per line, ISO-8601 timestamps.\n- `--tsv`: tab-separated with a header line, epoch-ms timestamps.\n- `--json` wins if both `--json` and `--tsv` are passed.\n\n## `--since` parsing\nAccept `Nh` / `Nd` / `Nm` and absolute ISO-8601 / epoch-ms.\n\n## Acceptance\n- `cotails history` lists the ~100 sessions active in 24h on the live DB in under 200ms.\n- `cotails history --json` and `--tsv` pipe cleanly.\n- `--directory` and `--limit` filter correctly.\n- Verified SQL is in the design doc.\n","status":"open","priority":2,"issue_type":"feature","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-31T04:01:43Z","created_by":"rektide de la faye","updated_at":"2026-07-31T04:01:43Z","dependencies":[{"issue_id":"cotail-history-cmd","depends_on_id":"cotail-history","type":"parent-child","created_at":"2026-07-31T00:01:51Z","created_by":"rektide de la faye","metadata":"{}"},{"issue_id":"cotail-history-cmd","depends_on_id":"cotail-subcommands","type":"blocks","created_at":"2026-07-31T00:01:59Z","created_by":"rektide de la faye","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"cotail-subcommands","title":"Restructure into src/ layout with entry point, bin, and subcommand dispatch","description":"Restructure from a single flat `opencotails.ts` into a domain-grouped `src/` layout, give it one working entry point, a real installable `cotails` command, and subcommand dispatch. Project is \u003c1.0, so breaking changes are fine.\n\n## Naming / spelling (canonicalize)\n- Package: **opencoattails** (2t — matches the local dir; the current `opencotails` in package.json is a typo). CLI: **cotails**.\n- Fix `package.json`: name `opencoattails`; drop bogus `\"main\": \"index.js\"` and `\"module\": \"opencotails.js\"`; add `\"bin\": { \"cotails\": \"src/cli.ts\" }`; realistic 0.x version; real description/keywords.\n- README: consistent `opencoattails` package / `cotails` command; examples use `cotails search ...`.\n- **Human action (flagged, not auto):** GitHub repo is `rektide/opencotails` (1t). Rename on GitHub to `opencoattails` and update the local remote URL.\n\n## Target layout\n```\nsrc/\n├── cli.ts                # ONE entry: shebang, main(), dispatch, top-level help\n├── format.ts             # renderTable / emitJsonl / emitTsv + ANSI\n├── opencode/             # abstractions · wrappers · types (version-agnostic)\n│   ├── db.ts             #   discoverDb, openReadOnly, registerRegex\n│   ├── types.ts          #   Session, MessageCounts, SearchHit, SearchTerms, HistoryFilter\n│   ├── source.ts         #   Source interface + detectSources() factory\n│   ├── v1/source.ts      #   v1 impl: message / part queries\n│   └── v2/source.ts      #   v2 impl: session_message / event queries\n└── commands/\n    ├── search.ts         # cotails search\n    └── history.ts        # cotails history (stub until cotail-history-cmd)\n```\nRoot `opencotails.ts` is deleted; its contents split across `cli.ts`, `opencode/db.ts`, `opencode/types.ts`, `opencode/v1/source.ts`, `opencode/v2/source.ts`, `commands/search.ts`.\n\n## Version seam (design decision)\nA single DB can be mixed (v1-native + v2-native sessions during transition), so `Source` is **per-version** (`v1/` and `v2/` each implement it); `detectSources(db)` returns the set present. Commands iterate available sources and merge (history sums counts; search unions hits). This retires the global `detectMode` boolean — querying both when both are present is a correctness win for mixed DBs.\n\n## Dispatch\n`main()` in `cli.ts`: `argv[0]` → `search` | `history`; bare `cotails` or unknown subcommand prints usage (lists subcommands) and exits non-zero. `history` may print \"not implemented\" until cotail-history-cmd lands.\n\n## Run / install (no-build preserved)\n- Node 22+ type-stripping: `node src/cli.ts` runs directly.\n- `bin: cotails → src/cli.ts` with `#!/usr/bin/env node` shebang → `pnpm link --global` puts `cotails` on PATH.\n\n## Acceptance\n- `cotails search foo bar` matches today's `cotails foo bar` output (identical on v1-only DBs; may improve on mixed DBs).\n- `cotails` alone prints help listing subcommands and exits non-zero.\n- `cotails history` is recognized (stub ok).\n- `pnpm link --global` installs a working `cotails` on PATH.\n- No `opencotails.ts` at root; no `index.js`/`opencotails.js` references remain.\n- `package.json` validates (name opencoattails, bin cotails→src/cli.ts, no fictional fields).\n","status":"open","priority":2,"issue_type":"feature","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-31T04:01:26Z","created_by":"rektide de la faye","updated_at":"2026-07-31T04:43:17Z","dependencies":[{"issue_id":"cotail-subcommands","depends_on_id":"cotail-history","type":"parent-child","created_at":"2026-07-31T00:01:51Z","created_by":"rektide de la faye","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"cotail-history","title":"Recent-session history viewer","description":"List sessions active within a time cutoff (default 24h) so a user can orient on *what they were recently working on and where* — complementing `search` (find by content) with `history` (find by recency).\n\nUnlike search, history reads only session/message metadata (no body scans), so it needs **no FTS index** and runs in milliseconds against the live DB regardless of size.\n\nDesign: `.design/history-viewer/design.md` (status: decided). Verified query on the live DB: 101 sessions in 24h, sub-100ms.\n\n## Scope (children)\n- `cotail-subcommands` — introduce explicit subcommand dispatcher (prerequisite; breaking change: `cotails foo` becomes `cotails search foo`).\n- `cotail-history-cmd` — implement `cotails history`: cutoff filter, directory/title/recent+total message counts, table/json/tsv output.\n\n## Out of scope\n- Message-body snippets (search owns that), live `--follow` (needs the CDC engine), transcript dump (`cotail-dump` epic), FTS index (`cotail-index`).\n","status":"open","priority":2,"issue_type":"epic","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-31T04:01:12Z","created_by":"rektide de la faye","updated_at":"2026-07-31T04:01:12Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"cotail-fileuse-tool","title":"File-use search: tool-call accesses","description":"List/search file paths referenced by tool calls across history.\n\n- Extract path-like fields from tool-call parts (v1: part.data with $.type='tool', args at $.state.input — e.g. Read/Edit/Write filePath, Glob/Grep pattern/path).\n- Optional glob match against the path.\n- EXCLUDE paths under the session's own directory (session.directory) — cross-project focus.\n- Group by session; show the session directory + the out-of-project paths hit.\n\nAcceptance: a command lists sessions and the out-of-project file paths their tools touched, with glob filtering. v1 part reader first.","status":"open","priority":2,"issue_type":"feature","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-27T10:55:25Z","created_by":"rektide de la faye","updated_at":"2026-07-27T10:55:25Z","dependencies":[{"issue_id":"cotail-fileuse-tool","depends_on_id":"cotail-xproject","type":"parent-child","created_at":"2026-07-27T06:55:27Z","created_by":"rektide de la faye","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"cotail-xproject","title":"Cross-project file use \u0026 session relocation","description":"Work often spans projects: a session opened under project A reads/edits files in project B, then a new session gets created under B to continue. There's currently no way to see that cross-project activity, so sessions end up **misplaced** — filed where work *started* rather than where it *landed*.\n\nTwo phases joined by that use case:\n\n1. **Discovery (read-side):** a file-use search listing file paths a session touches via tool (then shell) activity, with optional glob, **excluding paths inside the session's own `directory`**. A synthesis view ranks sessions by out-of-project activity — surfacing relocation candidates.\n2. **Action (write-side):** move or copy a misplaced session into its true target project.\n\n## Scope (children)\n- `cotail-fileuse-tool` — file-use search over tool-call accesses (foundation).\n- `cotail-misplaced` — misplaced-session finder (payoff of the above).\n- `cotail-fileuse-shell` — extend file-use to shell-command path tokens.\n- `cotail-copy-session` — copy a session into another project (safe, ships first).\n- `cotail-move-session` — move/reassign a session's project (destructive, last).\n\n## Out of scope\nSearch/matching semantics (`cotail-search`), transcript dump (`cotail-dump`), FTS index (`cotail-index`).\n\n## Notes\n- (1)-(3) are read-side and can ship on the current v1 DB now — tool data lives in `part` rows (`$.type='tool'`, args at `$.state.input`).\n- Full v2 support needs a `session_message` reader (gap surfaced by `cotail-v2-impact`, not yet ticketed).\n- (4)-(5) mutate opencode's source data; wait for the Rust/effect decision and a careful look at opencode's session/project model.\n","notes":"Scope refined: the write-side actions (copy/move) now live under cotail-session-mgmt (the dedicated write-side epic). This epic is now DISCOVERY-ONLY: file-use search (tool + shell) + the misplaced-session finder. The misplaced finder feeds session-mgmt's move/copy as the trigger for action.","status":"open","priority":2,"issue_type":"epic","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-27T10:55:11Z","created_by":"rektide de la faye","updated_at":"2026-07-27T10:56:24Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"cotail-session-mgmt","title":"Session management \u0026 reparenting (write-side)","description":"The **write-side** counterpart to the read-side search/display epics: mutate session attribution in opencode's SQLite DB.\n\nToday `cotails` is strictly read-only. opencode's CLI offers no `session move` (only `list`/`delete`), so reparenting currently means hand-rolled SQL. This epic owns safe, scripted mutation of session→project attribution.\n\n## Scope\n- **Reparent / move** — reassign a session's `project_id` (and optionally `directory`/`path`) to fix mis-attribution. Concrete trigger: coredns-dnse had 4 sessions scattered across 3 project rows (`601ddab2` = ~/src, `b663feea` = coredns-dnse, `global`); manually UPDATEd to unify under `b663feea`.\n- **Project cleanup** — detect and reconcile duplicate/overlapping `project` rows for the same worktree (e.g. `global` mistakenly carrying a project-specific worktree).\n- **Dry-run / preview** — show the before/after diff before writing.\n- **WAL-safety discipline** — short write transactions with `busy_timeout`, `BEGIN IMMEDIATE`, open per-op not held open. Document the conflict risk against the live opencode writer.\n\n## Out of scope\n- Read-side reporting (see `cotail-display`) and search/scoping (`cotail-search`).\n- General SQLite admin (vacuum, checkpoint) — only session-attribution mutation.\n\n## Reference\n- opencode DB write warning: `~/archive/doc/opencode-store.md` §4 \"What to avoid\".\n- Proven safe in practice: 2026-07-25 live 2-row UPDATE on `opencode-.db` while opencode ran, zero conflict (`busy_timeout=10000`, `BEGIN IMMEDIATE`).\n- Session schema: `session(project_id, directory, path, parent_id, fork_session_id, …)`.","notes":"Added children: cotail-copy-session (safe, ships first) and cotail-move-session (destructive, builds on copy). Trigger context: cotail-xproject's misplaced-session finder surfaces the sessions these actions refile. cotail-move-session is the ticket form of this epic's existing reparent/move scope.","status":"open","priority":2,"issue_type":"epic","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-25T02:28:58Z","created_by":"rektide de la faye","updated_at":"2026-07-27T10:56:24Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"cotail-v2-impact","title":"Understand opencode v2 impact on cotails","description":"Research spike: map opencode v2's data model and determine what v2 already provides, so the session-dump epic (`cotail-dump`) and the broader cotails feature set are grounded in v2 reality.\n\nQuestions:\n1. Does v2 already dump/export transcripts? (avoid duplicating)\n2. What is the canonical opencode transcript format?\n3. v2 session/message/part data model vs v1 (`session`/`message`/`part`/`event`).\n4. Message ordering \u0026 ordinal feasibility for range selection.\n5. Net impact on cotails (schema, storage location, event-vs-table model).\n\nDeliverable: `.design/v2.md` at the repo root.\n","notes":"Research complete; .design/v2.md written.\n\nKey findings:\n- v2 has NO CLI transcript dump (gap confirmed) — only a TUI palette action using formatSessionTranscript (packages/tui/src/routes/session/index.tsx:3034-3057). cotails fills a real gap.\n- Canonical dump format = markdown (# title + meta block + ## User/## Shell/## Assistant joined by ---). cotails should match it for byte-compatibility.\n- v2 message store = session_message table (packages/core/src/session/sql.ts:109-128); stable ordinal = seq (projector.ts:443-458). Revert creates gaps, so rank by position not raw seq.\n- CRITICAL: v2-native sessions leave message/part EMPTY (only SessionV1.Event.Created emitted, session.ts:374). cotails v1 reader will silently miss v2 content -\u003e need a session_message reader with v1 fallback.","status":"closed","priority":2,"issue_type":"task","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-24T10:39:21Z","created_by":"rektide de la faye","updated_at":"2026-07-24T10:46:29Z","closed_at":"2026-07-24T10:46:29Z","close_reason":"Closed","dependencies":[{"issue_id":"cotail-v2-impact","depends_on_id":"cotail-dump","type":"parent-child","created_at":"2026-07-24T06:39:21Z","created_by":"rektide de la faye","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"cotail-dump","title":"Session transcript dump","description":"Dump a session's full transcript to stdout (or a file), ideally in **opencode's transcript format** so output is recognizable and portable.\n\n## Range selection (composable)\n- **By message ordinal** — e.g. `--from 5 --to 12` (5th through 12th messages, inclusive).\n- **By date** — relative windows, e.g. `--from 2mo --to 2d` = everything from 2 months ago through 2 days ago, inclusive. Support units `h`, `d`, `mo` (and absolute dates if easy).\n- **Mix both** — ordinal and date ranges applied together, ideally at different segments (e.g. ordinals 1-10 within a date window, or several disjoint ranges).\n\n## Target\nThis ticket targets **opencode v2** (`~/a/a/v2` = `/home/rektide/archive/anomalyco/v2`) — NOT what we run yet, but the intended target. See `.design/v2.md` (produced by `cotail-v2-impact`) for the v2 data model + transcript format, and to confirm v2 doesn't already ship a dump we'd be duplicating.\n\n## Depends on\n- `cotail-v2-impact` — research prerequisite.\n","notes":"Confirmed via cotail-v2-impact: v2 ships no CLI dump (TUI-only), so this epic fills a real gap. Target format = formatSessionTranscript markdown (packages/tui/src/routes/session/index.tsx:3034-3057); read v2 from session_message (seq ordinal), fall back to message/part for v1. See .design/v2.md.","status":"open","priority":2,"issue_type":"epic","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-24T10:39:20Z","created_by":"rektide de la faye","updated_at":"2026-07-24T10:46:33Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"cotail-basic-fields","title":"Show other low-effort session fields","description":"Several useful columns on the `session` table are a single SELECT + render away (no joins, no new logic). Batch them as optional display enrichments:\n\n- `model` — model name (e.g. `glm-5.2`).\n- `agent` — agent name.\n- `version` — opencode version that ran the session.\n- `cost` (real) — session cost in USD.\n- `tokens_input`, `tokens_output`, `tokens_reasoning`, `tokens_cache_read`, `tokens_cache_write` — token usage.\n- `summary_files`, `summary_additions`, `summary_deletions` — git diff summary stats.\n- `share_url` — share link (if set).\n- `workspace_id` — workspace.\n\nImplementation notes:\n- All are direct columns on `session`; add to the existing SELECTs in `buildTitleQuery` / `buildPartQuery`.\n- Render compactly in human view (consider a `--verbose`/`-v` flag to avoid clutter, or a `--fields` selector). Always include in `--json`.\n- Each can land independently if desired.\n\nAcceptance: at least model + cost + token totals render in `--json` and (optionally, behind a flag) in human output.\n","status":"open","priority":2,"issue_type":"feature","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-24T10:35:05Z","created_by":"rektide de la faye","updated_at":"2026-07-24T10:35:05Z","dependencies":[{"issue_id":"cotail-basic-fields","depends_on_id":"cotail-display","type":"parent-child","created_at":"2026-07-24T06:35:07Z","created_by":"rektide de la faye","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"cotail-search","title":"Search \u0026 scoping","description":"The **query/matching read-side**: how users find and scope sessions against the source opencode DB (and later the FTS index).\n\nScope:\n- **Regex matching** — DONE (case-insensitive JS regex via custom `re()`; `-F`/`--fixed-strings`, `-s`/`--case-sensitive`).\n- **Term semantics** — currently strict AND of all terms; consider OR, phrase/exact, negation.\n- **Scoping filters** — `--directory`, `--session \u003cid\u003e`, `--project \u003cid\u003e` (source schema supports all three via `session.directory` / `session.id` / `session.project_id`).\n- **Type filter** — text/reasoning/tool exists; consider message-role scoping.\n\nOut of scope: reporting/display fields (see `cotail-display`) and the FTS index write-path (see `cotail-index`). Scoping-by-directory is the filter counterpart to the `cotail-directory` display ticket.\n","status":"open","priority":2,"issue_type":"epic","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-24T10:34:48Z","created_by":"rektide de la faye","updated_at":"2026-07-24T10:34:48Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"cotail-fork-point","title":"Show last fork point (prev reply \u003c-\u003e fork reply, with timestamps)","description":"Given `session.fork_message_id` (the message at the divergence boundary), render the two turns flanking it:\n\n- the **previous prompt + reply** immediately before the boundary, with its `time_created` stamp;\n- the **fork prompt + reply** at/after the boundary, with its `time_created` stamp.\n\nFetch turns from `message`/`part` (v1) or `event` filtered to `message.part.updated.1` (v2) — see the README \"V2 event fallback\" section for the table/field mapping.\n\nAcceptance: for a forked session, both boundary turns render with timestamps in human and json output.","status":"open","priority":2,"issue_type":"feature","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-24T10:17:35Z","created_by":"rektide de la faye","updated_at":"2026-07-24T10:17:35Z","dependencies":[{"issue_id":"cotail-fork-point","depends_on_id":"cotail-display","type":"parent-child","created_at":"2026-07-24T06:17:40Z","created_by":"rektide de la faye","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"cotail-fork-time","title":"Show last fork time","description":"A session forks via `session.fork_session_id` (parent) + `session.fork_message_id` (divergence point).\n\n\"Last fork time\" = the most recent fork event touching this session. Two interpretations:\n1. This session forked from its parent (its own `time_created` when `fork_session_id` is set).\n2. A child forked FROM this session (`max(time_created)` over sessions where `fork_session_id = this.id`).\n\nDecision needed: default to interpretation 2 (latest fork touching the session); show interpretation 1 when set.\n\nAcceptance: fork time rendered (human + json) where fork lineage exists; omitted when none.","status":"open","priority":2,"issue_type":"feature","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-24T10:17:34Z","created_by":"rektide de la faye","updated_at":"2026-07-24T10:17:34Z","dependencies":[{"issue_id":"cotail-fork-time","depends_on_id":"cotail-display","type":"parent-child","created_at":"2026-07-24T06:17:40Z","created_by":"rektide de la faye","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"cotail-created-at","title":"Show session first-created time","description":"Add `session.time_created` to results alongside the existing `updated` timestamp.\n\n- Human view: distinguish created vs updated (show both compactly).\n- `--json`: expose a `created` field.\n\nAcceptance: first-created timestamp from `session.time_created` appears in both human and json output.","notes":"Implemented in opencotails.ts:\n- Added `created` = datetime(time_created/1000) to both SELECTs.\n- renderHuman shows `created → updated` when they differ, else the single timestamp.\n- `--json` exposes `created` (alongside `updated`).\n\nVerified across part search, --title-only, and --json.","status":"closed","priority":2,"issue_type":"feature","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-24T10:17:33Z","created_by":"rektide de la faye","updated_at":"2026-07-24T10:35:44Z","closed_at":"2026-07-24T10:35:44Z","close_reason":"Closed","dependencies":[{"issue_id":"cotail-created-at","depends_on_id":"cotail-display","type":"parent-child","created_at":"2026-07-24T06:17:39Z","created_by":"rektide de la faye","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"cotail-copy-session","title":"Copy a session into another project","description":"Non-destructive: clone a session (and its fork lineage?) into a target project/directory — reassigning project_id/directory on the copy, original intact. Decide whether the copy lives in opencode's DB (new session) or a cotails-owned store. Ship before move (safe, reversible).\n\nAcceptance: copy \u003csession\u003e --to \u003cproject/dir\u003e produces a session filed under the target without altering the original.","status":"open","priority":3,"issue_type":"feature","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-27T10:55:27Z","created_by":"rektide de la faye","updated_at":"2026-07-27T10:55:27Z","dependencies":[{"issue_id":"cotail-copy-session","depends_on_id":"cotail-session-mgmt","type":"parent-child","created_at":"2026-07-27T06:56:22Z","created_by":"rektide de la faye","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"cotail-move-session","title":"Move a session to another project","description":"Destructive: reassign a session's project_id/directory to a target (≈ copy + delete original, or a direct update). Safeguards: dry-run, confirmation, move record. Depends on cotail-copy-session.\n\nAcceptance: move \u003csession\u003e --to \u003cproject/dir\u003e refiles the session under the target; original removed/archived with confirmation.","status":"open","priority":3,"issue_type":"feature","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-27T10:55:27Z","created_by":"rektide de la faye","updated_at":"2026-07-27T10:55:27Z","dependencies":[{"issue_id":"cotail-move-session","depends_on_id":"cotail-session-mgmt","type":"parent-child","created_at":"2026-07-27T06:56:23Z","created_by":"rektide de la faye","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"cotail-fileuse-shell","title":"File-use search: shell-command accesses","description":"Extend file-use extraction to shell commands: parse the command string for path tokens (args of cd/cat/cp/mv/editors, redirects). Same glob + exclude-in-project filtering as cotail-fileuse-tool. Lower frequency than tool accesses but catches activity the structured tools miss.\n\nDepends on cotail-fileuse-tool.\n\nAcceptance: shell-command path tokens appear in the file-use listing alongside tool paths.","status":"open","priority":3,"issue_type":"feature","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-27T10:55:26Z","created_by":"rektide de la faye","updated_at":"2026-07-27T10:55:26Z","dependencies":[{"issue_id":"cotail-fileuse-shell","depends_on_id":"cotail-xproject","type":"parent-child","created_at":"2026-07-27T06:55:28Z","created_by":"rektide de la faye","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"cotail-misplaced","title":"Misplaced-session finder","description":"Synthesis view over file-use data: rank sessions by the share/count of file activity OUTSIDE session.directory. Surface \"filed under A but most access is in B\" candidates, inferring the likely target directory from the dominant external paths. Default threshold + override flag.\n\nDepends on cotail-fileuse-tool.\n\nAcceptance: a command lists sessions whose out-of-project file activity exceeds a threshold, with the inferred target directory.","status":"open","priority":3,"issue_type":"feature","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-27T10:55:26Z","created_by":"rektide de la faye","updated_at":"2026-07-27T10:55:26Z","dependencies":[{"issue_id":"cotail-misplaced","depends_on_id":"cotail-xproject","type":"parent-child","created_at":"2026-07-27T06:55:28Z","created_by":"rektide de la faye","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"cotail-rust","title":"Port cotails to Rust","description":"Rewrite cotails as a Rust project: the opencode source DB and the cotails FTS index read via the `turso` crate (or `rusqlite`), clap-v4 CLI, figment2 config, tracing. Rust is a natural fit for a fast local search/index tool and matches the broader toolchain.\n\nCandidate scope:\n- Read-only source-DB access (regex + display) reimplemented in Rust.\n- Phase 1 FTS index (`cotail-index`) as a separate owned DB.\n- Incremental indexing + `status`.\n\n## ⚠️ Language decision is UNRESOLVED\nWe have NOT decided whether cotails's future home is **Rust** or **effect.ts**. This epic and `cotail-effect` are the two parallel candidate paths; at most one will proceed.\n\n### Before starting this epic, ask the user:\n1. **Commit for real?** — are we committing to Rust as the production path, or\n2. **Prototype only?** — just spike it in Rust to learn/compare, or\n3. **Hold and compare?** — pause and weigh it against the effect.ts path first.\n\nDo not begin implementation until the user has answered. See the twin epic `cotail-effect`.\n","status":"open","priority":3,"issue_type":"epic","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-24T10:34:23Z","created_by":"rektide de la faye","updated_at":"2026-07-24T10:34:23Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"cotail-effect","title":"Port cotails to effect.ts","description":"Adopt [effect.ts](https://effect.website) as the future home for cotails: structured concurrency, Layers/Services for the DB + index + config, typed errors, and a config layer (figment2/clap-style ergonomics on the TS side).\n\nCandidate scope:\n- Model the opencode source DB and the cotails FTS index DB as Effect Services/Layers.\n- Port the existing regex search + display features into the Effect app.\n- Build the Phase 1 FTS index path (`cotail-index`) on top.\n\n## ⚠️ Language decision is UNRESOLVED\nWe have NOT decided whether cotails's future home is **Rust** or **effect.ts**. This epic and `cotail-rust` are the two parallel candidate paths; at most one will proceed.\n\n### Before starting this epic, ask the user:\n1. **Commit for real?** — are we committing to effect.ts as the production path, or\n2. **Prototype only?** — just spike it in effect.ts to learn/compare, or\n3. **Hold and compare?** — pause and weigh it against the Rust path first.\n\nDo not begin implementation until the user has answered. See the twin epic `cotail-rust`.\n","status":"open","priority":3,"issue_type":"epic","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-24T10:34:22Z","created_by":"rektide de la faye","updated_at":"2026-07-24T10:34:22Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"cotail-index","title":"FTS index (Phase 1)","description":"**Phase 1: the FTS index write-path.** Move from ~5s regex scans of the 4.8 GB source DB to milliseconds-scale FTS5 MATCH.\n\nScope:\n- Owned cotails index DB (~/.local/share/cotails/index.db, Turso/SQLite FTS5) — see README architecture + planned schema.\n- `cotails index` — build + incremental indexing keyed on `session.time_updated`.\n- Scopable indexing: --session, --directory, --project, --since.\n- `cotails status` — coverage, freshness, stale-session count, index size.\n\nProduces the index that `cotail-search` queries. Engine choice (Rust turso vs effect.ts) is gated by `cotail-rust` / `cotail-effect`.\n","status":"open","priority":4,"issue_type":"epic","owner":"rektide+git@voodoowarez.com","created_at":"2026-07-24T10:34:52Z","created_by":"rektide de la faye","updated_at":"2026-07-24T10:39:16Z","dependency_count":0,"dependent_count":0,"comment_count":0}
